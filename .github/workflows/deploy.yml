name: Deploy

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      environment:
        description: Environment
        required: true
      access_key:
        description: access
        required: true
      secret_key:
        description: secret
        required: true
      region:
        description: region
        required: true

env:
  ENVIRONMENT: ${{ github.event.inputs.environment || 'master' }}
  AWS_ACCESS_KEY_ID: ${{ github.event.inputs.access_key || '123' }}
  AWS_SECRET_ACCESS_KEY: ${{ github.event.inputs.secret_key || '123' }}
  AWS_REGION: ${{ github.event.inputs.region || 'eu' }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Configure AWS Amplify
        uses: ambientlight/amplify-cli-action@0.3.0
        with:
          amplify_command: configure
          amplify_env: ${{ env.ENVIRONMENT }}
        env:
          AWS_ACCESS_KEY_ID: ${{ env.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ env.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ env.AWS_REGION }}
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
      - name: Deploy project
        uses: ambientlight/amplify-cli-action@0.3.0
        with:
          amplify_command: publish
          amplify_env: ${{ env.ENVIRONMENT }}
        env:
          AWS_ACCESS_KEY_ID: ${{ env.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ env.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ env.AWS_REGION }}